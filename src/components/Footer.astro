---
import { footerLinks, socialLinks } from '../config/header-footer';
import { siteConfig } from '../config/siteconfig';
---

<footer class="bg-white border-t border-gray-200 mt-12">
  <div class="container mx-auto px-4 py-6">
    <div class="flex flex-wrap justify-between items-center">
      <nav class="w-full md:w-auto mb-4 md:mb-0">
        <ul class="flex flex-wrap justify-center md:justify-start">
          {footerLinks.map((link) => (
            <li class="mx-2 my-1">
              <a href={link.href} class="text-sm text-gray-600 hover:text-main-color transition duration-300">{link.text}</a>
            </li>
          ))}
          <li class="mx-2 my-1">
            <a href="/rss.xml" class="text-sm text-gray-600 hover:text-main-color transition duration-300">RSS</a>
          </li>
        </ul>
      </nav>
      <div class="flex justify-center md:justify-end space-x-4">
        {socialLinks.map((link) => (
          <a href={link.href} class="text-main-color hover:opacity-80 transition duration-300" aria-label={link.label}>
            <span class="sr-only">{link.label}</span>
            <svg class="w-5 h-5" aria-hidden="true" fill="currentColor">
              <use xlink:href={`/sprite.svg#${link.icon}`}></use>
            </svg>
          </a>
        ))}
      </div>
    </div>
    <div class="mt-6 text-center text-xs text-gray-500">
      <p>&copy; {new Date().getFullYear()} {siteConfig.siteName}. Tous droits réservés.</p>
    </div>
  </div>
</footer>

<style>
  .text-main-color {
    color: var(--main-color);
  }
</style>
